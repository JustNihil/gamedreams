<!DOCTYPE html>
<html>
<head>
  
</head>
<body>
<style>
h1 {
  color: #333;
  font-size: 24px;
  margin-bottom: 10px;
}

span {
  font-weight: bold;
}

a {
  color: #333;
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

.notice {
  color: #008000;
  font-weight: bold;
}

.alert {
  color: #ff0000;
  font-weight: bold;
}
</style>
<h1>Профиль</h1>
<%= link_to 'Главная', products_path %>

<span>Здравствуйте, <%= current_user.first_name %> <%= current_user.last_name %></span>
<%= link_to 'Выйти', destroy_user_session_path, data: { turbo_method: :delete } %>
<%= link_to 'Редактировать профиль', edit_user_registration_path %>

<h2>Мои игры</h2>

<div id="products">
  <% @products.each do |product| %>
    <%= render product %>
    <p>
      <%= link_to "Подробнее", product %>
    </p>
  <% end %>
</div>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
</body>
</html>