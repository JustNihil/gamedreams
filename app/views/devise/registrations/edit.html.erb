<!DOCTYPE html>
<html>
<head>
  
</head>
<body>
<style>
h2 {
  color: #333;
  font-size: 20px;
  margin-bottom: 10px;
}

.field {
  margin-bottom: 20px;
}

.label {
  font-weight: bold;
}

.actions {
  margin-top: 10px;
}

.actions input[type="submit"] {
  background-color: #008000;
  color: #fff;
  padding: 10px 20px;
  border: none;
  font-weight: bold;
  cursor: pointer;
}

.actions input[type="submit"]:hover {
  background-color: #006400;
}

h3 {
  color: #333;
  font-size: 18px;
  margin-bottom: 10px;
}

.delete-button {
  background-color: #ff0000;
  color: #fff;
  padding: 5px 10px;
  border: none;
  font-weight: bold;
  cursor: pointer;
}

.delete-button:hover {
  background-color: #8b0000;
}

.back-link {
  display: block;
  margin-top: 20px;
  color: #333;
  text-decoration: underline;
}
</style>

<h2>Редактировать профиль</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :password %> <i>(оставьте пустым если не собираетесь изменять)</i><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> знаков минимум</em>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password" %>
  </div>
  <div class="field">
  <%= f.label :first_name %><br />
  <%= f.text_field :first_name,  value: @user.first_name%>
  </div>
  <div class="field">
  <%= f.label :last_name %><br />
  <%= f.text_field :last_name, value: current_user.last_name, autocomplete: "last_name" %>
  </div>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<h3>Удалить аккаунт</h3>

<div>Прощай аккаунт <%= button_to "Удалить", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete %></div>

<%= link_to "На главную", :back %>

</body>
</html>